export const sessionStorageGetItem=(key:string)=>{
    return sessionStorage.getItem(key)
}
export const sessionStorageSetItem=(key:string,values:string)=>{
    return sessionStorage.setItem(key,values)
}